{{- range .Values.microservices }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  labels:
    app: {{ .name }}
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: {{ .containerPort }}
  selector:
    app: {{ .name }}
{{- end }}
