/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: '/management-frontend', // Präfix für statische Assets
  basePath: '/management-frontend',
  output: 'standalone',
};

export default nextConfig;
