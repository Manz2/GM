apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.propertyBackendStaging.name }}
  labels:
    app: {{ .Values.propertyBackendStaging.name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.propertyBackendStaging.containerPort }}
  selector:
    app: {{ .Values.propertyBackendStaging.name }}
