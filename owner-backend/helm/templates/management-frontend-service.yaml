apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.managementFrontendStaging.name }}
  labels:
    app: {{ .Values.managementFrontendStaging.name }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: {{ .Values.managementFrontendStaging.containerPort }}
  selector:
    app: {{ .Values.managementFrontendStaging.name }}
